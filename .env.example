# Database Configuration
# Default is SQLite for local development
DATABASE_URL=sqlite:///./data/app.db

# Example PostgreSQL (with pgvector extension)
# DATABASE_URL=postgresql://user:password@localhost:5432/quant_trading

# Frontend Configuration (Optional if running separately)
# VITE_API_URL=http://localhost:8000
